scripts/fast.sh	BOARD_CONSOLE_ACTIONS	garde-fou 4 : l'axe surveille le pouvoir de la console, plus la presence du lien
main/board_common.h	BOARD_HAS_SD	le brochage reste inconditionnel, seuls le sondage au demarrage et les commandes console dependent de cet axe
main/security/sec_confirm.c	sec_confirm_arm(uint8_t slot, sec_op_t op, uint32_t now_ms)	diverge de KeSp_firmware par un parametre : l'ecran doit nommer l'operation armee, pas seulement son slot — divergence assumee (Tache 1, ecran OLED)
scripts/check.sh	FAST_RUN_CMD="./scripts/fast.sh"	mode maison : check.sh est un fichier template que /tripwire:init reecrit, donc les garde-fous materiels vivent dans scripts/fast.sh — cette ligne est le point d accroche
scripts/check.sh	( ./scripts/full.sh )	mode maison : le build des trois cartes vit dans scripts/full.sh, pas dans la boucle ALL_VARIANTS du template (vide ici) — un re-scaffold standard le remplacerait en silence
main/security/sec_confirm.c	sec_confirm_arm_named	l ecran doit nommer le COMPTE OATH demande, pas seulement l operation — sans quoi l appui est un interrupteur de presence et pas un accord ; meme motif que le parametre op deja diverge de KeSp
main/security/oath_proto.c	if (type != OATH_ALGO_TOTP_SHA1) return sw_only(out, cap, SW_NOT_SUPPORTED);	SHA-256 est REFUSE (6A81) alors que la spec l annonce « dedans » : implementer un algorithme qu aucun compte reel n exerce serait speculatif, et un HMAC-SHA256 non teste rendrait des codes faux pour toujours sans qu une erreur le dise. A rouvrir si l export Proton des douze comptes en contient un — voir la section « Portee » de docs/superpowers/specs/2026-08-18-oath-totp-design.md
